<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block w-full px-3 py-2 bg-zinc-800 p-2 rounded-md outline-none" type="text" name="task"
                    id="task" placeholder="Enter your task">
                <textarea class="block w-full  mt-2 px-3 py-2 bg-zinc-800 rounded-md outline-none" name="details" id=""
                    placeholder="write your task details here"></textarea>
                <input type="submit" class="mt-2 bg-blue-600 px-5 py-2 rounded-md" value="Create task">
            </form>
        </div>
        <div class="task  flex gap-4 flex-wrap p-10">
            <% if(files.length>0){%>
                <% files.forEach(function(val) { %>
                    <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md">
                        <div class="text-white text-3xl tracking-tighter">
                            <%= val %>
                        </div>
                        <a class="text-blue-500 mt-3" href="/files/<%= val %>">Read More.</a>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h3 class="text-zinc-600">no task here</h3>
                            <% } %>
        </div>
    </div>
</body>

</html>