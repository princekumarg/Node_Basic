<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-full bg-zinc-900 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/">Back To Home</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3">All Users</h1>
        <hr>
        <div class="users flex gap-3 flex-wrap">
            <% if(users.length>0){%>
                <% users.forEach(function(users) {%>
                    <div class="user w-72 h-126 p-10 bg-zinc-800 rounded-lg">
                        <div class="w-full h-72 rounded-lg bg-red-500">
                            <img src="<%= users.imageurl %>" alt="" class="w-full h-full object-cover rounded-lg">
                        </div>
                        <h3 class="text-2xl mt-3 tracking-tighter">
                            <%= users.name %>
                        </h3>
                        <h5 class="text-zinc-500">
                            <%=users.email%>
                        </h5>
                        <div class="flex gap-3 mt-10">
                            <a href="" class="text-zinc-500">Edit User</a>
                            <a href="/delete/<%= users._id%>" class="text-zinc-300">Delete User</a>
                        </div>
                    </div>
                    <% }); %>
                        <%} else{ %>
                            <a class="text-blue-500" href="/">Create user please</a>
                            <%}%>

        </div>
    </div>
</body>

</html>